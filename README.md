# lorogue
A small ESP32S3 enabled device, with LoRa connectivity.

This PCB/Schematic uses an Wio-S3 from SEEED Studio, a module that contains a LoRa module and a ESP32S3 MCU.

USB C connectivity along with an ESD protection.

Cool "minimalengineering" backdesign.

minimal_engineering on instagram
